<% if flash.any? %>
  <div class="flashes alert">
    <% user_facing_flashes.each do |key, value| -%>
      <div class="flash-<%= key %> text-center lead">
        <%= value %>
      </div>
    <% end -%>
  </div>
<% end %>
